// Criar uma interface
// A interface define um "molde" para o objeto usuário

export interface Usuario {
    nome: string;
    email: string;
    profissao: string;
    idade: number;
    ativo: boolean;
}